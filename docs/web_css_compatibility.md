# Web CSS Compatibility

The `web-css-vars` compatibility note now lives inside the shared backend
compatibility contract:

- [`docs/backend_compatibility.md`](backend_compatibility.md)

See the `web-css-vars` row and section there for:

- browser feature floors
- practical alpha guidance
- fallback expectations
- the distinction between what Paint validates and what consumers still own
