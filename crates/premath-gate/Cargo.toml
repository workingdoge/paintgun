[package]
name = "premath-gate"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Premath gate witness and admission kernels"
rust-version = "1.89"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
