# Archived v1 Integration Tests

These tests exercise archived/kernel-era KCIR behavior and are kept for
reference while the active suite focuses on v2 profile-aware flows.

Cargo only runs files directly under `tests/` as integration tests, so files in
this directory are intentionally excluded from default `cargo test`.
